# 临时文件目录

此目录用于存储文档分析过程中生成的临时数据。

## 说明

- 所有临时文件（如 `tmp_*.json`、`tmp_*.txt` 等）都应存储在此目录下
- 此目录中的文件会被自动忽略，不会提交到版本控制系统
- 可以安全删除此目录中的所有文件，不会影响 skill 的功能

## 常见临时文件

- `tmp_extract.json` - 文档内容提取结果
- `tmp_*.json` - 各类中间处理数据
- `tmp_*.txt` - 文本提取临时文件
